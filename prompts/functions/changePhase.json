{
    "name": "should_send_reminder",
    "description": "Analyze the last message and decide if the run operation should send any message or not",
    "parameters": {
      "type": "object",
      "properties": {
        "state": {
          "type": "boolean",
          "description": "if the boolean is true that means that its worth to send the reminder message to the user. If state is false then dont send any reminder nessage to the user"
        }
      },
      "required": [
        "state"
      ]
    }
}